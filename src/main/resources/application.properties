#spring.application.name=EdHouse-Final-v4
#spring.datasource.url=jdbc:postgresql://localhost:5432/EHouseTP
#spring.datasource.username=postgres
#spring.datasource.password=1234
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.hibernate.ddl-auto=create-drop
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true
#server.port=8080
##No envia el trace al usuario cuando hay error.
#server.error.include-stacktrace=never
##spring.jackson.date-format=yyyy-dd-MM

#ip.frontend=localhost:4200
#jwt.secret=0C/ZZj9OCW7Q74lUM200BayaFq6u12EKyps4NLFQNufowubX5nE1DQT8XRdwv0zUBp7Tns1yhYlwzUCm3maQkg==



##########################
## Base de datos
#spring.datasource.url=${SPRING_DATASOURCE_URL}
#spring.datasource.username=${POSTGRES_USER}
#spring.datasource.password=${POSTGRES_PASSWORD}

## Puerto dinámico asignado por Render
#server.port=${PORT:8080}

## JWT Secret
#jwt.secret=${JWT_SECRET}

## URL del Frontend
#ip.frontend=${FRONTEND_URL}

#############################

# Configuración de la aplicación
spring.application.name=EdHouse-Final-v4

# Configuración de la base de datos
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${POSTGRES_USER}
spring.datasource.password=${POSTGRES_PASSWORD}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Puerto dinámico asignado por Render
server.port=${PORT:8080}

# Configuración de JWT
jwt.secret=${JWT_SECRET}

# URL del Frontend
ip.frontend=${FRONTEND_URL}

# Configuración de errores
server.error.include-stacktrace=never

# Fecha (si es necesario)
# spring.jackson.date-format=yyyy-dd-MM
